apiVersion: v1
kind: Service
metadata:
  name: alicious_text_feature_project
  namespace: itzchaktunik-dev
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 3307
      targetPort: 3307
      nodePort: 30007 # Optional, for NodePort type
  selector:
    app: alicious_text_feature_project